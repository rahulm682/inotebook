import React from 'react'

const About = () => {
    return (
        <div>
            This is an note making react app.
            <p>
                It allows user to make notes and perform the operation. User can update the note according to their requirements. Also if task is completed, the notes can be deleted.
            </p>
        </div>
    )
}

export default About
